# Threshold / suppression file — starts empty.
# Populate with entries like:
#   suppress gen_id 1, sig_id 2019401
#   threshold gen_id 1, sig_id 2001219, type both, track by_src, count 10, seconds 60
# Tune as rules become noisy.
