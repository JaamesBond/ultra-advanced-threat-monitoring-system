name: Terraform Deploy (v8)

on:
  push:
    branches: [main]
    paths:
      - "new-infra/**"
  workflow_dispatch:

env:
  AWS_ACCOUNT_ID: "286439316079"
  AWS_REGION: "eu-central-1"

jobs:
  # ---------------------------------------------------------------
  # Step 1: bc-ctrl (The Brain) — VPC + EKS + runner VM
  # Runs on ubuntu-latest because bc-ctrl EKS has public endpoint.
  # K8s manifests (Wazuh manager/indexer/dashboard) applied here.
  # ---------------------------------------------------------------
  ctrl-plane:
    name: Deploy Control Plane (The Brain)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::286439316079:role/GitHubActionsDeployRole
          aws-region: eu-central-1

      - uses: hashicorp/setup-terraform@v3

      - name: "Terraform Apply (Ctrl) — Stage 1: Network"
        working-directory: new-infra/environments/bc-ctrl/eu-central-1
        run: |
          terraform init
          terraform apply -auto-approve \
            -target=module.vpc \
            -target=module.peering \
            -target=aws_security_group.fck_nat \
            -target=aws_iam_role.fck_nat \
            -target=aws_iam_role_policy_attachment.fck_nat_ssm \
            -target=aws_iam_instance_profile.fck_nat \
            -target=aws_instance.fck_nat \
            -target=aws_route.private_nat \
            -target=aws_route53_zone.bc_ctrl_internal \
            -target=aws_security_group.github_runner \
            -target=aws_iam_role.github_runner \
            -target=aws_iam_role_policy_attachment.runner_admin \
            -target=aws_iam_instance_profile.github_runner \
            -target=aws_instance.github_runner \
            -target=aws_security_group.security_tools \
            -target=aws_iam_role.security_tools \
            -target=aws_iam_role_policy_attachment.ssm \
            -target=aws_iam_instance_profile.security_tools \
            -target=aws_instance.security_tools \
            -target=tls_private_key.wazuh_dashboard \
            -target=tls_self_signed_cert.wazuh_dashboard \
            -target=aws_acm_certificate.wazuh_dashboard

      - name: "Terraform Apply (Ctrl) — Stage 2: EKS"
        working-directory: new-infra/environments/bc-ctrl/eu-central-1
        run: |
          terraform apply -auto-approve -target=module.eks

      - name: Install kubectl (pre-Stage-3 cleanup)
        uses: azure/setup-kubectl@v4

      - name: Configure kubectl for cleanup (bc-ctrl-eks)
        run: aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name bc-uatms-ctrl-eks

      - name: Cleanup stale Helm releases (pre-Stage-3)
        run: |
          helm uninstall aws-load-balancer-controller -n kube-system 2>/dev/null || true
          helm uninstall external-secrets -n external-secrets 2>/dev/null || true
          helm uninstall cert-manager -n cert-manager 2>/dev/null || true

      - name: "Terraform Apply (Ctrl) — Stage 3: Reconcile"
        working-directory: new-infra/environments/bc-ctrl/eu-central-1
        run: |
          terraform apply -auto-approve

      - name: Install kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubectl (bc-ctrl-eks)
        run: aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name bc-uatms-ctrl-eks

      # Terraform apply installs LBC/external-secrets/cert-manager via Helm (atomic).
      # Wait for webhooks to be Ready before applying Wazuh (uses Certificate + ExternalSecret CRDs).
      - name: Wait for addon webhooks
        run: |
          kubectl -n cert-manager rollout status deployment/cert-manager-webhook --timeout=120s
          kubectl -n external-secrets rollout status deployment/external-secrets-webhook --timeout=120s

      - name: Deploy Wazuh Manager stack (bc-ctrl)
        run: |
          CERT_ARN=$(terraform -chdir=new-infra/environments/bc-ctrl/eu-central-1 output -raw wazuh_dashboard_cert_arn)
          kubectl kustomize new-infra/k8s/wazuh \
            | sed 's/\${AWS_ACCOUNT_ID}/${{ env.AWS_ACCOUNT_ID }}/g' \
            | sed "s|\${WAZUH_DASHBOARD_CERT_ARN}|${CERT_ARN}|g" \
            | kubectl apply -f -

  # ---------------------------------------------------------------
  # Step 2: bc-prd (The Data) — EKS + security stack + sensors
  # Runs on self-hosted runner in bc-ctrl VPC (reaches private
  # bc-prd EKS endpoint via VPC peering).
  # K8s manifests (Wazuh agent, Zeek, Suricata) applied here.
  # ---------------------------------------------------------------
  production-plane:
    name: Deploy Production Plane (The Data)
    needs: ctrl-plane
    runs-on: self-hosted
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::286439316079:role/GitHubActionsDeployRole
          aws-region: eu-central-1

      - uses: hashicorp/setup-terraform@v3

      - name: Terraform Apply (Prd)
        working-directory: new-infra/environments/bc-prd/eu-central-1
        run: |
          terraform init
          terraform apply -auto-approve

      - name: Configure kubectl (bc-prd-eks)
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name bc-uatms-prd-eks --kubeconfig /tmp/kubeconfig-prd
          echo "KUBECONFIG=/tmp/kubeconfig-prd" >> $GITHUB_ENV

      - name: Verify kubectl config (bc-prd-eks)
        run: |
          echo "KUBECONFIG=$KUBECONFIG"
          kubectl config view --minify
          kubectl config current-context
          kubectl get nodes --request-timeout=10s

      # Bootstrap Cilium CRDs before applying Wazuh/Zeek/Suricata so
      # NetworkPolicy resources can be validated against the live API.
      - name: Bootstrap CRDs (Helm)
        working-directory: new-infra/environments/bc-prd/eu-central-1
        run: |
          terraform apply -auto-approve \
            -target=helm_release.cilium \
            -target=helm_release.falco \
            -target=helm_release.tetragon \
            -target=helm_release.external_secrets \
            -target=aws_iam_role.external_secrets \
            -target=aws_iam_role_policy.external_secrets_secrets_manager

      - name: Wait for CRDs to propagate
        run: sleep 60

      - name: Wait for external-secrets webhook
        run: kubectl -n external-secrets rollout status deployment/external-secrets-webhook --timeout=120s

      - name: Deploy Wazuh Agent (bc-prd)
        run: |
          kubectl kustomize new-infra/k8s/wazuh/agent \
            | sed "s/\${AWS_ACCOUNT_ID}/${{ env.AWS_ACCOUNT_ID }}/g" \
            | kubectl apply -f -

      - name: Deploy Zeek (bc-prd)
        run: |
          kubectl kustomize new-infra/k8s/zeek \
            | sed "s/\${AWS_ACCOUNT_ID}/${{ env.AWS_ACCOUNT_ID }}/g" \
            | kubectl apply -f -

      - name: Deploy Suricata (bc-prd)
        run: |
          kubectl kustomize new-infra/k8s/suricata \
            | sed "s/\${AWS_ACCOUNT_ID}/${{ env.AWS_ACCOUNT_ID }}/g" \
            | kubectl apply -f -
